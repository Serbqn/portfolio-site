# Admin password (required). Set a strong password.
ADMIN_PASSWORD=change-me-please

# Secret used to sign admin JWTs (required). Use a long random string.
ADMIN_TOKEN_SECRET=replace-with-a-long-random-string-at-least-32-chars

# Optional: contact form forwarding
CONTACT_FORM_EMAIL=hello@serb.design
